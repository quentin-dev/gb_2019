image: sunbr0/gb_img:996914c9

stages:
    - build

rom:
    stage: build
    script:
        - git submodule update --init
        - make lib
        - make
    artifacts:
        paths:
            - build/game.gb
